export function Product({ name, description, price }) {
  return (
    <div>
      <h1>Product #1</h1>
      <p>Product name: {name}</p>
      <p>Description: {description}</p>
      <p>Price: {price}</p>
    </div>
  );
}
